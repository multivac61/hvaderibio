on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:
  schedule:
    - cron: "0 * * * *" # Run every hour
name: Deploy
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request'
    steps:
      - uses: DeterminateSystems/nix-installer-action@main
      - uses: DeterminateSystems/magic-nix-cache-action@main
      - uses: actions/checkout@main

      - name: 📚 Install node dependencies
        run: nix develop -c pnpm install

      - name: 🎬 Scrape movie data
        run: nix develop -c pnpm scrape

      - name: ☑️ Check svelte-kit, format and linting
        run: nix develop -c pnpm check

      - name: 🔨 Build for Cloudflare
        run: nix develop -c pnpm build
        env:
          ADAPTER: cloudflare

      - name: 🚀 Deploy to Cloudflare Pages
        run: nix develop -c npx wrangler pages deploy .svelte-kit/cloudflare --project-name=hvaderibio
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
